#include "test.h"

void QuicklySort(int *array, int size)
{
	//1.按照基准值进行划分，---基准值的位置div
	//  Partion(array,left,right);
	//2.quickSort(array,left,div);
	//3.quickSort(array,div+1,right);
	//4.两个指针，找到不符合的时候停下来，交换继续进行，
	//5.	
}

int main()
{


	system("pause");
	return 0;
}